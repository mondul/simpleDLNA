using System;
using System.Runtime.InteropServices;

// Assembly-wide attributes shared by every project, linked in via
// Directory.Build.props. Everything else that used to live in
// GlobalAssemblyInfo.cs is now expressed as MSBuild properties and emitted by
// the SDK's generated assembly info.

[assembly: ComVisible(false)]
[assembly: CLSCompliant(true)]
